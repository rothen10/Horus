# This file contains example environment variables
# Copy this file to .env.local and fill in the actual values

# Theme and Language Settings
# These are managed client-side in localStorage
# Default theme: 'dark' or 'light' (user preference is saved)
# Default language: 'fr' (French) or 'en' (English)
NEXT_PUBLIC_DEFAULT_THEME=dark
NEXT_PUBLIC_DEFAULT_LANGUAGE=fr

# API Configuration (if needed for future features)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
